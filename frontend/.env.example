# Leave empty to use the Vite dev proxy (see vite.config.ts) which forwards
# /api requests to http://localhost:8000 during local development.
# In production, point this at your deployed backend URL, e.g.:
# VITE_API_BASE=https://leadpilot-api.onrender.com
VITE_API_BASE=
